# DO NOT USE THIS FILE DIRECTLY!
# This is a TEMPLATE that shows the structure of hardware-configuration.nix
# 
# TO GENERATE YOUR ACTUAL HARDWARE CONFIGURATION:
# 1. Boot into NixOS (installer or existing system)
# 2. Run: sudo nixos-generate-config --dir /path/to/this/directory
# 3. This will create the correct hardware-configuration.nix for YOUR hardware
#
# NEVER copy hardware-configuration.nix from:
# - Another machine
# - GitHub repository  
# - Backup from different hardware
#
# The UUIDs and device paths MUST match your actual hardware or the system WILL NOT BOOT!

{ config, lib, pkgs, modulesPath, ... }:

{
  imports = [ (modulesPath + "/installer/scan/not-detected.nix") ];

  # THIS IS A TEMPLATE - THESE ARE PLACEHOLDER VALUES
  # DO NOT USE THESE UUIDs - RUN nixos-generate-config!
  
  boot.initrd.availableKernelModules = [ "xhci_pci" "ahci" "nvme" "usb_storage" "sd_mod" ];
  boot.initrd.kernelModules = [ ];
  boot.kernelModules = [ "kvm-intel" ];
  boot.extraModulePackages = [ ];

  fileSystems."/" = {
    device = "/dev/disk/by-uuid/XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX";
    fsType = "ext4";
  };

  fileSystems."/boot" = {
    device = "/dev/disk/by-uuid/XXXX-XXXX";
    fsType = "vfat";
  };

  swapDevices = [
    { device = "/dev/disk/by-uuid/XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX"; }
  ];

  # WARNING: THIS FILE WILL NOT WORK!
  # Generate your real hardware-configuration.nix with:
  # sudo nixos-generate-config --dir .
}